import React from 'react';
import ArtDisplay from '../components/artDisplay';

const Home = () => {
    return (
    <div className='container'>
        <ArtDisplay />
    </div>
    );
};

export default Home;